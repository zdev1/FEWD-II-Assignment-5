/*eslint-env browser*/
var $ = function (x) {
    "use strict";
    return window.document.getElementById(x);
};

window.addEventListener("click", function () {
    "use strict";
    init(x);
});

function init() {
    "use strict";
    
    window.document.getElementById("calculator").addEventListener("click", function() {
        if (target.x !== "calculator" & target.x!== "equal"){
            
            enter(e.target.valueOf);
        } else if (target.x === "equal")
    }
    function enter (value) {
        $(result).nodeValue;
    
    function calculate () {
        $("result").nodeValue = eval("result").value;
    }
 
